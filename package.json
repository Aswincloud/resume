{
  "name": "aswin-resume",
  "private": true,
  "type": "module",
  "scripts": {
    "embed": "node scripts/embed.mjs",
    "prebuild": "npm run embed",
    "dev": "npm run embed && wrangler dev",
    "predeploy": "npm run embed",
    "deploy": "wrangler deploy",
    "types": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260828.1",
    "typescript": "^7.0.2",
    "wrangler": "^4.127.1"
  },
  "dependencies": {
    "@cloudflare/puppeteer": "^1.4.0"
  }
}
